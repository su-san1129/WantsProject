export class ResourcePath {
    public static readonly URL = 'http://localhost:8080/api';
    public static readonly USERS = `${ResourcePath.URL}/users`;
    public static readonly USER_GROUPS = `${ResourcePath.URL}/user_groups`;
}